const { getRandomId } = require('./get-random-id/get-random-id.helper');
const {
  getRandomTitle,
} = require('./get-random-title/get-random-title.helper');

module.exports = {
  getRandomId,
  getRandomTitle,
};
